<% if params[:tag] %>
  Text for "<%= params[:tag] %>" tag
<% end %>

<%= unless @poems.empty? 
      render 'poems/index'
    end %>


<%= unless @short_stories.empty? 
      render 'short_stories/index' 
    end %>

<%= unless @authors.empty?
      render 'authors/index' 
    end %>
