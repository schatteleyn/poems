<%= form_for(@poem) do |f| %>
  <% if @poem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poem.errors.count, "error") %> prohibited this poem from being saved:</h2>

      <ul>
      <% @poem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container">
    <%= f.hidden_field :author_id, value: current_author.id %>

    <div><%= f.label :title %><br />
    <%= f.text_field :title, autofocus: true %></div>

    <div><%= f.label :metaphor %><br />
    <%= f.text_field :metaphor %></div>

    <div><%= f.label :content %><br />
    <%= f.text_area :content %></div>

    <%= f.submit %>
  </div>
<% end %>
